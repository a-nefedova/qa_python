test_add_new_book_add_two_books(self)
    (пример теста)
    Добавляет 2 книги в пустой словарь.
    Проверяет, что длина словаря = 2.

test_add_new_book_not_add(self, name)
    Добавляет 2 книги в словарь, обе не валидны по длине названия.
    Проверяет, что после добавления книг длина словаря не изменилась.

test_set_book_genre_set(self)
    Устанавливает валидный жанр существующей в словаре книге.
    Проверяет, что метод возвращает установленный жанр.

test_set_book_genre_not_set(self)
    Устанавливает не валидный жанр существующей в словаре книге.
    Проверяет, что в словаре названию этой книге соответствует пустая строка в качестве жанра.

test_get_book_genre_got_genre(self)
    Проверяет, что метод возвращает корректный жанр книги.

test_get_books_with_specific_genre_got_two_books(self)
    Проверяет, что метод возвращает 2 книги жанра "Ужасы"

test_get_books_genre_got_dict(self)
    Добавляет в словарь две книги, устанавливает им жанры.
    Проверяет, что содержимое словаря соответствует установленным данным.

test_get_books_for_children_got_books_wo_age_rating(self)
    Проверяет, что метод возвращает список книг, жанры которых не содержатся в списке жанров с возрастным рейтингом

test_add_book_in_favorites_add_two_books(self)
    Добавляет 2 книги из словаря в список избранных.
    Проверяет, что длина списка избранных = 2.

test_delete_book_from_favorites_got_one_book(self)
    Добавляет 2 книги из словаря в список избранных. Удаляет одну книгу из списка.
    Проверяет, что длина списка избранных = 1.

test_get_list_of_favorites_books_got_list(self)
    Добавляет 2 книги из словаря в список избранных.
    Проверяет, что метод возвращает список с добавленными книгами.